import styles from './styles.module.scss';

const BookForm: React.FC = () => {
  return (
    <div className={`block ${styles.bookForm}`}>
      <h2>BookForm</h2>
    </div>
  );
};

export { BookForm };
